{
  "name": "f1-race",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot serve.ts",
    "build": "bun run build.ts",
    "lint": "oxlint src/",
    "fmt": "oxfmt --write src/",
    "fmt:check": "oxfmt src/"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxlint": "^1.58.0",
    "oxfmt": "^0.43.0",
    "typescript": "^5"
  }
}
